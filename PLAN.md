# PLAN.md

开工前读这个。**当前状态 + 下一轮范围。**

---

## 当前状态(2026-09-01 20:10,最后一次 agent 回合)

- **全部工作已 commit 并 push**,本地与 `origin/main` 齐平。最后一笔是
  `fb11a2f`「步骤 2:把 scene-invariants 的六条不变量转绿,预算公式仲裁出
  三个设计决定」。`pnpm check` 绿:**7 文件 76 测试**(下面「现在做出了
  什么」里的 69 是旧数)。
- **本地部署验证已过**:`pnpm build` 绿;`pnpm preview` 起在 4173,
  agent-browser 拍过 1920×1080 与 390×844,画面正常、交互在首屏之内、
  `ab errors` 空、`ab a11y` 0 violations / 0 incomplete。
  `vite.config.ts` 是 `base: "./"` 且 index.html 在根目录,所以 Pages 的
  子路径不用改配置,`./card.png` 也解得对。
- **线上还没有东西**,而且这是有意的:仓库 private 到 cutoff(见文末),
  `checks.yml` 每个 job 都是 `if: !private`,所以 `fb11a2f` 那次 run 的状态
  是 `skipped`。**周三 12:00 前必须跑 `/comp4020:ship`**,否则被批改的那个
  网址不存在。这一步要作者本人拍板,agent 不擅自翻公开。
- **card.png 与 description meta 已是本项目的**(`2a83cac`,通关画面),
  不必再做。
- `reflections/crit-5.md` 和 `PROCESS.md` 仍然只能作者本人写。PROCESS.md
  的第四段素材:传感器逼出的预算仲裁(池底进关卡数据 / 渠 5 mesh /
  兽 8 primitive),对应 commit 就是 `fb11a2f`。

---

## 下一轮:视觉(2026-09-01 晚拟,**先同意再动手**)

先核过的现状,不是印象:`play.ts:52` 一直在切 `.flowered` 类,但 `styles.css`
里没有这个类,`scene.ts` 也不认识它 —— **通关是完全不可见的**。所以下面第 1 条
不是「不够好看」,是规格 §6 没有实现。

按「规格没实现 > 画面读不通 > 便宜 > 贵」排序。截止前只来得及一件的话,做第 1 条。

### 1. 通关反馈:椰枣与藤蔓(§6,规格硬要求)

- **做什么**:水流经过的每一层长出植株。`scene.ts` 给每个 platform / pool
  预置一组植株 mesh(默认 `visible = false`),`setFoliage(ports)` 按水已经
  流经的层级打开。树干用 Cylinder,冠用压扁的 Sphere,藤蔓用细 Box ——
  都在现有的 primitive 白名单里。颜色走 `PALETTE.date` 三档,那是整个调色板
  里唯一还没用上的一档。
- **不新增关卡数据**:长在哪儿由已有的 `platforms` / `pools` id 决定(水流经
  过的那些),所以 §3.5「视觉复杂度以关卡数据封顶」仍然成立,渲染器没有即兴
  发明任何东西。
- **前景垂叶放到最后**:§6 说解开时枝叶垂到镜头前,但同一节也写明「只有解开
  之后才允许遮挡,玩家还在解的时候不许盖住任何承重接缝」。所以它必须由通关
  状态门控,而且是这一条里最后做的部分 —— 做不完就砍它,每层长出植株已经
  足够回答「我赢了没有」。
- **传感器**:`scene-invariants` 的 `BUDGET` 加一项 `foliage`;新测试钉两条
  —— 开局所有植株 `visible === false`(§6「开局台面是裸石」),通关后可见的
  植株数 > 0。前景垂叶只在通关状态下存在。

### 2. 露台落地(§3.5「侧面是主体的实心挤出」)

- **做什么**:柱廊拆掉之后,y=1 那几块露台底下什么都没有,是飘着的 —— 截图里
  一眼就看得见。主体从顶面挤到地面,而不是固定 0.34 厚。
- **怎么做**:`terrace()` 接一个高度参数,高度由 layout 里那个 port 的 y 推出来。
  那是**尺寸**,不是位置,§3.1 允许(「对齐靠改几何的尺寸或原点,不靠挪位置」)。
- **传感器**:新测试 —— 每块 platform 的包围盒底面必须落到 y ≤ 0,没有悬空的
  露台。

### 3. 取景(便宜,收益直接)

- **做什么**:45° 下大池甩到画面左边,中间一大片空 ochre,整体偏小。只动
  `CAMERA.halfHeight` 和相机看向的点,不动任何模型。
- **传感器**:新测试 —— 四个角度下每个 port 的投影都落在视锥内,并留一格边距。
  这条能防住以后加关卡时「某个角度下有东西被切出画面」。

### 4. 穆什胡什造型(最贵,放最后)

- **做什么**:现在是占位球体。规格要伊什塔尔门浮雕的读法:侧面剪影、长颈、
  蛇头,保留「身子侧面、眼睛正面」的错误透视。
- **怎么做**:先在 8 个 primitive 的预算内重排比例(拉长身体、抬颈、收窄头)。
  真的不够就把 `BUDGET.beast` 调高,并在那条注释里写清多出来的每一件是什么、
  为什么 —— 预算是决定,不是观察值。
- **传感器**:`BUDGET.beast` 本身,外加一条「兽的包围盒不超过 1 tile 见方、
  高约 1 TILE」(§3.5)。

### 动手前要你拍板的两件

- **A. 画布底色。**§3.7 写的是暖沙 `#F2E4CE`,现在是 `--ochre-mid`。
  `styles.css` 里留了理由:沙色底会和沙岩露台同色,水和台面都陷进背景。
  保持 ochre(那就要在 CLAUDE.md §3.7 里记一笔为什么偏离),还是改回规格?
- **B. 枝叶要多密。**§6 的原话是「枝叶填满画面」。要真做到填满(含前景垂叶),
  还是止步于「每层长出植株、看得出这一层通过水了」?前者更贴规格,后者风险低
  且不会碰遮挡那条红线。

---

## 现在做出了什么

**第一关能玩了。** `pnpm dev` → `http://localhost:5173/`

三个动词,一关演完:

1. **走** — 点兽走得到的地方,它走过去(不是瞬移)
2. **引水** — 站在池子上按空格,水流进渠里 **停住**,末端悬空
3. **转视角** — 点画面空处 / 左右方向键,断开的那截接上大池,再引水,大池满

`69` 个测试全绿,`ab a11y` 零违规零 incomplete,1920×1080 与 390×844 都不溢出。
`card.png` 已做(通关画面)。

---

## 代码结构

| 文件 | 职责 | 红线 |
|---|---|---|
| `src/rules.ts` | 五条规则,纯查表 | **不许出现任何坐标**。出现了就是上游错了 |
| `src/levels/level1.ts` | 关卡数据 + 摆放,手写字面量 | 拓扑(`level1`)和坐标(`level1Layout`)分开放 |
| `src/iso.ts` | 等距投影、绕轴旋转 | 只用于**画**和**检查**,绝不参与判定 |
| `src/config/style.ts` | 全部可调参数 | 逻辑代码不许硬编码任何数值 |
| `src/scene.ts` | Three.js:几何、材质、相机、动画 | 不做任何连通判定 |
| `src/play.ts` | 输入、DOM 按钮、游戏循环 | 拾取用 `project`,不用射线检测 |

**数据结构的关键一条**:每条连接带 `when: { camera?, turns? }`,省略即「不关心」。
相机和砖块两种机制**同一套判定**吃得下,而且可以要求同时满足:

```ts
{ between: ["ledge", "stair"], when: { camera: 135, turns: { elbow: 0 } } }
```

这个抽象已经兑现过一次:相机被拿掉又加回来时,`rules.ts` 和全部传感器一行没改。
**不要把它拍平成单一条件。**

---

## 传感器(`spec/`)

连通是声明的,所以**运行时任何代码都不许推导它**。但测试可以反过来查:

- 每条声明的水路,两端投影必须落在同一像素(< 0.05)
- 走路的连接只需**挨着**(< 1.0)—— 这是两套阈值,不是一套。渠口接池必须
  像素级重合,相邻地砖挨着就行。合并它们会两头出错
- **没声明**的配置下,那两端必须确实分开(> 0.6),否则玩家会看到「明明接上了
  却不流」
- 声明的水流方向必须真的往画面下方走
- 半满的渠**不可行走**;灌满**不可逆**,而可行走**是**可逆的
  ——这两条最容易被一次「顺手统一一下」删掉,它们是这个游戏本身

---

## 待办

| | 谁 | 说明 |
|---|---|---|
| `reflections/crit-5.md` | **只能作者写** | `check:evidence` 卡着它。两个问题:突破是什么;这份工作改变了你想成为什么样的开发者。150–300 字 |
| 通关反馈 | 待做 | 规格要求「水流经的层级长出椰枣树与藤蔓,枝叶填满画面」。现在只有大池水面出现,**玩家不会知道自己赢了** |
| 空间法则改造 | **部分完成,见下** | 方案 A 已定;柱廊/砖缝/0.42 已拆,平台实心化待做 |
| 第二至四关 | 待做 | 引擎和传感器就位,只差数据。**建议排在通关反馈之后** —— 一关完整比四关半截强 |
| 穆什胡什造型 | 待做 | 现在是占位:小、圆、头身比大。规格要伊什塔尔门浮雕、侧面剪影、保留「身侧面眼正面」的错误透视 |

---

## 空间法则改造(决定已定:方案 A)

`CLAUDE.md` 3.1/3.5 节把这几条当**建模纪律**执行,不是重写渲染层:整数坐标、
单一投影、统一 tile 尺寸,排序仍交给 WebGL 深度缓冲(本来就没手动排序,这条
从来不冲突)。方案 B(自己写 2D 等距 + 画家算法)已经排除。

拆除清单进度:

| 项目 | 状态 |
|---|---|
| 柱廊(`colonnade`/`drum`,每块露台下 8 根柱子) | **done** —— 函数和调用点都删了 |
| 顶面砖缝(`terrace()` 里那圈暗线) | **done** —— 规格明确归为调试渲染 |
| 渠道起点内缩 0.42(`rawSpan * 0.42` 的手调比例) | **done** —— 改用 `FORM.poolRadius`(同一个常数也用来画池子本体),`rawSpan/2` 只是防负长度的数学安全边界,不是观感数字 |
| 平台改成实心挤出体 | **待做** —— 柱廊一删,浮空的露台(`birth`/`walkway`/`spring` 等 y=1 的平台)底下什么都没有了,下一轮要么挤出到地面,要么另找视觉支撑手法 |

---

## 已知的坑

- **浏览器缓存**。这周有半小时耗在「画面和状态对不上」,真凶是 Chrome 缓存了旧
  JS —— 改了代码、reload 了、看到的还是旧画面,于是在修一个已经修好的东西。
  **诊断前先 Cmd+Shift+R,或者 URL 加 `?bust=N`。**
  `src/play.ts` 里留了 `window.__probe()`(仅 dev),一次把游戏状态和每个 port
  的屏幕坐标摆在一起,用来判断「游戏认为的」和「画出来的」是否一致。
- **dev server 端口**。之前同时跑过两个 vite(5173 / 5174),容易看错。
  现在只有一个,在 5173。
- **隐身方向就是视线方向**。屏幕上重合的两个东西必然一前一后。把目标放在
  渠口的**近端**,它会盖住渠;要放**远端**,水才像倒进去。传感器接不住这个
  ——它检查两点是否重合,而这个 bug 恰恰是重合得太彻底。前后关系只能靠眼睛。
- **俯角必须是 35.264°**(`atan(1/√2)`)。规格写 30–35,但只有这个值让整数格点
  **精确**重合;33° 给的补偿比是 2.178,两条边永远差一点。
- **每个相机角度的隐身方向不同**:45° 是 `(1,1,1)`,135° 是 `(-1,1,1)`,
  225° 是 `(-1,1,-1)`,315° 是 `(1,1,-1)`。当成常数写会红。

---

## 截止

**2026-09-02(周三)12:00**,墨尔本/堪培拉时间。crit 当天 14:00–15:30,
Marie Reay 4.03,导师 Bill McAlister。

仓库保持 private 到 cutoff,用 `/comp4020:ship` 翻公开并部署。
