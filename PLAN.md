# PLAN.md

开工前读这个。**当前状态 + 下一轮范围。**

---

## 现在做出了什么

**第一关能玩了。** `pnpm dev` → `http://localhost:5173/`

三个动词,一关演完:

1. **走** — 点兽走得到的地方,它走过去(不是瞬移)
2. **引水** — 站在池子上按空格,水流进渠里 **停住**,末端悬空
3. **转视角** — 点画面空处 / 左右方向键,断开的那截接上大池,再引水,大池满

`69` 个测试全绿,`ab a11y` 零违规零 incomplete,1920×1080 与 390×844 都不溢出。
`card.png` 已做(通关画面)。

---

## 代码结构

| 文件 | 职责 | 红线 |
|---|---|---|
| `src/rules.ts` | 五条规则,纯查表 | **不许出现任何坐标**。出现了就是上游错了 |
| `src/levels/level1.ts` | 关卡数据 + 摆放,手写字面量 | 拓扑(`level1`)和坐标(`level1Layout`)分开放 |
| `src/iso.ts` | 等距投影、绕轴旋转 | 只用于**画**和**检查**,绝不参与判定 |
| `src/config/style.ts` | 全部可调参数 | 逻辑代码不许硬编码任何数值 |
| `src/scene.ts` | Three.js:几何、材质、相机、动画 | 不做任何连通判定 |
| `src/play.ts` | 输入、DOM 按钮、游戏循环 | 拾取用 `project`,不用射线检测 |

**数据结构的关键一条**:每条连接带 `when: { camera?, turns? }`,省略即「不关心」。
相机和砖块两种机制**同一套判定**吃得下,而且可以要求同时满足:

```ts
{ between: ["ledge", "stair"], when: { camera: 135, turns: { elbow: 0 } } }
```

这个抽象已经兑现过一次:相机被拿掉又加回来时,`rules.ts` 和全部传感器一行没改。
**不要把它拍平成单一条件。**

---

## 传感器(`spec/`)

连通是声明的,所以**运行时任何代码都不许推导它**。但测试可以反过来查:

- 每条声明的水路,两端投影必须落在同一像素(< 0.05)
- 走路的连接只需**挨着**(< 1.0)—— 这是两套阈值,不是一套。渠口接池必须
  像素级重合,相邻地砖挨着就行。合并它们会两头出错
- **没声明**的配置下,那两端必须确实分开(> 0.6),否则玩家会看到「明明接上了
  却不流」
- 声明的水流方向必须真的往画面下方走
- 半满的渠**不可行走**;灌满**不可逆**,而可行走**是**可逆的
  ——这两条最容易被一次「顺手统一一下」删掉,它们是这个游戏本身

---

## 待办

| | 谁 | 说明 |
|---|---|---|
| `reflections/crit-5.md` | **只能作者写** | `check:evidence` 卡着它。两个问题:突破是什么;这份工作改变了你想成为什么样的开发者。150–300 字 |
| 通关反馈 | 待做 | 规格要求「水流经的层级长出椰枣树与藤蔓,枝叶填满画面」。现在只有大池水面出现,**玩家不会知道自己赢了** |
| 空间法则改造 | **部分完成,见下** | 方案 A 已定;柱廊/砖缝/0.42 已拆,平台实心化待做 |
| 第二至四关 | 待做 | 引擎和传感器就位,只差数据。**建议排在通关反馈之后** —— 一关完整比四关半截强 |
| 穆什胡什造型 | 待做 | 现在是占位:小、圆、头身比大。规格要伊什塔尔门浮雕、侧面剪影、保留「身侧面眼正面」的错误透视 |

---

## 空间法则改造(决定已定:方案 A)

`CLAUDE.md` 3.1/3.5 节把这几条当**建模纪律**执行,不是重写渲染层:整数坐标、
单一投影、统一 tile 尺寸,排序仍交给 WebGL 深度缓冲(本来就没手动排序,这条
从来不冲突)。方案 B(自己写 2D 等距 + 画家算法)已经排除。

拆除清单进度:

| 项目 | 状态 |
|---|---|
| 柱廊(`colonnade`/`drum`,每块露台下 8 根柱子) | **done** —— 函数和调用点都删了 |
| 顶面砖缝(`terrace()` 里那圈暗线) | **done** —— 规格明确归为调试渲染 |
| 渠道起点内缩 0.42(`rawSpan * 0.42` 的手调比例) | **done** —— 改用 `FORM.poolRadius`(同一个常数也用来画池子本体),`rawSpan/2` 只是防负长度的数学安全边界,不是观感数字 |
| 平台改成实心挤出体 | **待做** —— 柱廊一删,浮空的露台(`birth`/`walkway`/`spring` 等 y=1 的平台)底下什么都没有了,下一轮要么挤出到地面,要么另找视觉支撑手法 |

---

## 已知的坑

- **浏览器缓存**。这周有半小时耗在「画面和状态对不上」,真凶是 Chrome 缓存了旧
  JS —— 改了代码、reload 了、看到的还是旧画面,于是在修一个已经修好的东西。
  **诊断前先 Cmd+Shift+R,或者 URL 加 `?bust=N`。**
  `src/play.ts` 里留了 `window.__probe()`(仅 dev),一次把游戏状态和每个 port
  的屏幕坐标摆在一起,用来判断「游戏认为的」和「画出来的」是否一致。
- **dev server 端口**。之前同时跑过两个 vite(5173 / 5174),容易看错。
  现在只有一个,在 5173。
- **隐身方向就是视线方向**。屏幕上重合的两个东西必然一前一后。把目标放在
  渠口的**近端**,它会盖住渠;要放**远端**,水才像倒进去。传感器接不住这个
  ——它检查两点是否重合,而这个 bug 恰恰是重合得太彻底。前后关系只能靠眼睛。
- **俯角必须是 35.264°**(`atan(1/√2)`)。规格写 30–35,但只有这个值让整数格点
  **精确**重合;33° 给的补偿比是 2.178,两条边永远差一点。
- **每个相机角度的隐身方向不同**:45° 是 `(1,1,1)`,135° 是 `(-1,1,1)`,
  225° 是 `(-1,1,-1)`,315° 是 `(1,1,-1)`。当成常数写会红。

---

## 截止

**2026-09-02(周三)12:00**,墨尔本/堪培拉时间。crit 当天 14:00–15:30,
Marie Reay 4.03,导师 Bill McAlister。

仓库保持 private 到 cutoff,用 `/comp4020:ship` 翻公开并部署。
